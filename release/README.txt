VaultLock Release
=================

1. To run the application, open the 'VaultLock' folder and double-click 'VaultLock.exe'.
2. Your database 'vault.db' will be created in this folder (or 'VaultLock' folder depending on config).
3. Do not delete the internal files in 'VaultLock' directory.
